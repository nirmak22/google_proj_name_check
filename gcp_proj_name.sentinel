import "tfplan"


project_name_rule = rule {
    all tfplan.resources["google_project"].instances as _, r {
        r.attributes["name"] is string and r.attributes["name"].starts_with("kyn")
    }
}

main = rule { project_name_rule }